{{! NAVBAR - START }}
<nav class='navbar navbar-expand-lg navbar-light'>
  <div class='container-fluid'>
    {{! tree logo w/ title }}
    <a class='navbar-brand' href='#'>
      <img
        alt='tree'
        src='/images/treelogo.jpg'
        id='nav-tree'
        class='align-middle'
      /><a href='/' id='navbar-title'>We Help Austin</a>
    </a>
    <div class='collapse navbar-collapse nav-link-wrapper' id='navbarSupportedContent'>
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="#">Dashboard</a>
        </li>
       <li class="nav-item">
          <a class="nav-link" href="#">Profile</a>
        </li>
      </ul>
    </div>
    <div class='d-grid gap-2 d-md-flex justify-content-md-end'>
      {{#if loggedIn}}
      <form action='/'>
         <button id='logout-btn' class='btn btn-primary log-sign' type='submit'>Loggout</button>
      </form>
      {{else}}
      <form action='/login' />
         <button class='btn btn-primary log-sign' type='submit'>Log In</button>
      </form>
      <form action='/signup' />
         <button class='btn btn-primary log-sign' type='submit'>Sign Up</button>
      </form>
      {{/if}}
    </div>
  </div>
</nav>
{{! NAVBAR - END }}